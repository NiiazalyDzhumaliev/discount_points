<h1>Welcome</h1>
<% if logged_in? %>
    <h1>You are Logged In, <%= current_user.name %></h1> 
    <h1>You have, <%= current_user.point %></h1>   
    <%= link_to "Logout", logout_path%>    
    <%else%>
    <%= link_to "Login", login_path%>
<%= link_to "Sign Up", new_user_path%>
<%end%>